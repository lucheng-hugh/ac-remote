#pragma once

#include <Arduino.h>

const char kSetupPage[] PROGMEM = R"rawliteral(
<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>空调遥控器配网</title><style>
body{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#eef2f6;margin:0;padding:24px;color:#17202a}main{max-width:420px;margin:8vh auto;background:white;padding:26px;border-radius:20px;box-shadow:0 12px 40px #0002}h1{font-size:24px}label{display:block;margin:18px 0 6px;font-weight:600}input{box-sizing:border-box;width:100%;padding:13px;border:1px solid #ccd4dd;border-radius:11px;font-size:16px}button{width:100%;margin-top:22px;padding:14px;border:0;border-radius:11px;background:#1769ff;color:white;font-size:16px;font-weight:700}.hint{color:#667085;font-size:14px;line-height:1.55}</style></head>
<body><main><h1>连接家庭 Wi‑Fi</h1><p class="hint">请选择 2.4GHz Wi‑Fi。密码只保存在 ESP32 本机，不会写进源码。</p>
<form method="post" action="/api/wifi"><label>Wi‑Fi 名称</label><input name="ssid" maxlength="32" required><label>Wi‑Fi 密码</label><input name="password" type="password" maxlength="64"><button type="submit">保存并重启</button></form></main></body></html>
)rawliteral";

const char kWebUi[] PROGMEM = R"rawliteral(
<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>空调遥控器</title><style>
:root{color-scheme:light;--bg:#f3f5f8;--card:#fff;--text:#17202a;--muted:#667085;--blue:#1769ff;--line:#e3e7ed;--danger:#d92d20}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:720px;margin:auto;padding:18px 14px 50px}header{display:flex;justify-content:space-between;align-items:flex-start;margin:8px 4px 18px}h1{font-size:25px;margin:0 0 5px}.sub{color:var(--muted);font-size:13px;line-height:1.55}.pill{background:#e8f0ff;color:#1458cc;padding:7px 10px;border-radius:99px;font-size:12px}.card{background:var(--card);border-radius:20px;padding:18px;margin:14px 0;box-shadow:0 5px 22px #1018280c}.row{display:flex;align-items:center;gap:10px}.between{justify-content:space-between}.power{border:0;border-radius:50%;width:54px;height:54px;font-size:24px;background:#edf1f6}.power.on{background:var(--blue);color:white}.temp{font-size:58px;font-weight:700;letter-spacing:-4px}.temp small{font-size:20px;letter-spacing:0}.round{width:44px;height:44px;border:1px solid var(--line);background:white;border-radius:50%;font-size:24px}.modes{display:grid;grid-template-columns:repeat(5,1fr);gap:6px;margin:18px 0 14px}.modes button,.chip{padding:10px 5px;border:1px solid var(--line);background:white;border-radius:11px}.modes button.active,.chip.active{border-color:var(--blue);color:var(--blue);background:#edf4ff}label{display:block;color:var(--muted);font-size:13px;margin:13px 0 6px}select,input[type=time],input[type=text]{width:100%;padding:11px;border:1px solid var(--line);border-radius:11px;background:white;font-size:16px}.check{display:flex;align-items:center;gap:8px;color:var(--text);font-size:14px;margin:12px 0}.check input{width:18px;height:18px}.countdown{margin:12px 0;padding:12px 14px;border-radius:12px;background:#fff4e5;color:#9a4f00;font-size:15px;font-weight:700}.presence-device{display:grid;grid-template-columns:minmax(80px,.8fr) minmax(130px,1.3fr) auto;gap:7px;align-items:center;margin:8px 0}.presence-actions{display:flex;gap:8px;margin-top:10px;flex-wrap:wrap}.toggles{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:14px}.primary{width:100%;border:0;background:var(--blue);color:white;padding:14px;border-radius:12px;font-weight:700;font-size:16px;margin-top:16px}.secondary{border:1px solid var(--line);background:white;padding:9px 12px;border-radius:10px}.danger{color:var(--danger)}h2{font-size:18px;margin:0 0 12px}.days{display:grid;grid-template-columns:repeat(7,1fr);gap:5px}.day{position:relative}.day input{position:absolute;opacity:0}.day span{display:block;text-align:center;border:1px solid var(--line);border-radius:9px;padding:8px 2px;font-size:13px}.day input:checked+span{background:#edf4ff;border-color:var(--blue);color:var(--blue)}.schedule{border-top:1px solid var(--line);padding:13px 0}.schedule:first-child{border:0}.schedule-title{font-weight:700}.schedule-meta{font-size:13px;color:var(--muted);margin-top:4px}.empty{color:var(--muted);text-align:center;padding:18px}.toast{position:fixed;left:50%;bottom:22px;transform:translateX(-50%);background:#17202a;color:white;padding:10px 16px;border-radius:99px;opacity:0;pointer-events:none;transition:.2s}.toast.show{opacity:1}@media(min-width:620px){.toggles{grid-template-columns:repeat(4,1fr)}}
</style></head><body><main>
<header><div><h1>格力空调</h1><div class="sub" id="meta">正在连接设备…</div></div><div class="pill" id="source">--</div></header>
<section class="card"><div class="row between"><button id="power" class="power" aria-label="电源">⏻</button><div class="temp"><span id="temp">26</span><small>℃</small></div><div><button class="round" onclick="changeTemp(1)">＋</button><button class="round" onclick="changeTemp(-1)">－</button></div></div>
<div class="modes" id="modes"><button data-mode="auto">自动</button><button data-mode="cool">制冷</button><button data-mode="dry">除湿</button><button data-mode="fan">送风</button><button data-mode="heat">制热</button></div>
<label>风速</label><select id="fan"><option value="auto">自动</option><option value="low">低</option><option value="medium">中</option><option value="high">高</option><option value="max">强劲</option></select>
<div class="toggles"><button class="chip" data-key="swingVertical">上下扫风</button><button class="chip" data-key="swingHorizontal">左右扫风</button><button class="chip" data-key="quiet">静音</button><button class="chip" data-key="turbo">强劲</button></div>
<label>睡眠模式</label><select id="sleepMode"><option value="0">关闭</option><option value="1">睡眠 1</option><option value="2">睡眠 2</option><option value="3">睡眠 3</option><option value="4">睡眠 4</option></select>
<button class="primary" id="send">发送到空调</button></section>
<section class="card"><h2>新增定时任务</h2><label>执行时间</label><input type="time" id="scheduleTime" value="23:00"><label>重复日期</label><div class="days" id="days"></div>
<div class="row"><div style="flex:1"><label>动作</label><select id="schedulePower"><option value="on">开机</option><option value="off">关机</option></select></div><div style="flex:1"><label>温度</label><select id="scheduleTemp"></select></div></div>
<button class="primary" id="addSchedule">添加任务</button></section>
<section class="card"><div class="row between"><h2>定时任务</h2><button class="secondary" onclick="loadSchedules()">刷新</button></div><div id="scheduleList" class="empty">暂无任务</div></section>
<section class="card"><h2>自动离家 / 回家</h2><div class="sub" id="presenceStatus">正在读取状态…</div><div class="countdown" id="presenceCountdown" hidden></div><label class="check"><input type="checkbox" id="presenceEnabled">启用手机在家检测</label><label>家庭成员手机（最多 4 部）</label><div id="presenceDevices"></div><div class="presence-actions"><button class="secondary" id="addCurrentPhone" type="button">＋ 添加当前手机</button><button class="secondary" id="addBlankPhone" type="button">＋ 手动添加</button></div><label>全部离线多久判定离家</label><select id="presenceAway"><option value="300">5 分钟</option><option value="600" selected>10 分钟（推荐）</option><option value="900">15 分钟</option><option value="1800">30 分钟</option></select><label class="check"><input type="checkbox" id="presenceAutoOn" checked>第一部手机回家时自动开机</label><label class="check"><input type="checkbox" id="presenceAutoOff" checked>全部手机离家后自动关机</label><button class="primary" id="savePresence">保存自动控制</button><p class="sub">请分别用每部手机打开本页并点击“添加当前手机”，每次添加后保存。任意一部手机在线即视为有人在家；重启时不会因检测到在家而误开机，但全部离线倒计时结束后仍会正常关机。</p></section>
<section class="card"><h2>HomeKit / Siri</h2><div class="sub" id="homekitStatus">正在读取状态…</div><label>自定义配对码（8位数字）</label><input id="homekitCode" type="password" inputmode="numeric" maxlength="8" placeholder="请自行设置，不要发给别人"><button class="primary" id="saveHomekitCode">保存配对码</button><p class="sub">保存后，在 iPhone“家庭”中添加配件 → 更多选项 → 格力空调，并输入该配对码。</p></section>
</main><div class="toast" id="toast"></div><script>
const state={power:false,mode:'cool',temp:26,fan:'auto',swingVertical:true,swingHorizontal:false,quiet:false,turbo:false,light:true,xfan:false,sleepMode:0};let dirty=false;
const dayNames=['日','一','二','三','四','五','六'];
const sourceNames={boot:'启动状态',api:'网页/API',schedule:'定时任务',remote:'原装遥控器',homekit:'HomeKit/Siri','presence-arrive':'回家自动开启','presence-leave':'离家自动关闭'};
function toast(text){const el=document.getElementById('toast');el.textContent=text;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)}
async function api(path,options={}){const response=await fetch(path,{headers:{'Content-Type':'application/json'},...options});const data=await response.json();if(!response.ok||data.ok===false)throw new Error(data.error||'请求失败');return data}
function render(){document.getElementById('power').classList.toggle('on',state.power);document.getElementById('temp').textContent=state.temp;document.getElementById('fan').value=state.fan;document.getElementById('sleepMode').value=String(state.sleepMode||0);document.querySelectorAll('[data-mode]').forEach(b=>b.classList.toggle('active',b.dataset.mode===state.mode));document.querySelectorAll('[data-key]').forEach(b=>b.classList.toggle('active',!!state[b.dataset.key]))}
function changeTemp(delta){state.temp=Math.max(16,Math.min(28,state.temp+delta));dirty=true;render()}
async function loadState(){try{const data=await api('/api/state');if(!dirty){Object.assign(state,data.state);render()}document.getElementById('source').textContent=sourceNames[data.source]||data.source;document.getElementById('meta').textContent=`${data.time} · ${location.hostname}`}catch(e){document.getElementById('meta').textContent='设备连接失败'}}
async function sendState(){try{const data=await api('/api/control',{method:'POST',body:JSON.stringify(state)});Object.assign(state,data.state);dirty=false;render();toast('已发送')}catch(e){toast(e.message)}}
document.getElementById('power').onclick=()=>{state.power=!state.power;dirty=true;render()};document.getElementById('fan').onchange=e=>{state.fan=e.target.value;dirty=true};document.getElementById('sleepMode').onchange=e=>{state.sleepMode=Number(e.target.value);dirty=true};document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;dirty=true;render()});document.querySelectorAll('[data-key]').forEach(b=>b.onclick=()=>{state[b.dataset.key]=!state[b.dataset.key];dirty=true;render()});document.getElementById('send').onclick=sendState;
const days=document.getElementById('days');dayNames.forEach((name,i)=>days.insertAdjacentHTML('beforeend',`<label class="day"><input type="checkbox" value="${i}" checked><span>${name}</span></label>`));const tempSelect=document.getElementById('scheduleTemp');for(let t=16;t<=28;t++)tempSelect.add(new Option(`${t}℃`,t));tempSelect.value=26;
function daysText(mask){if(mask===127)return'每天';const names=[];dayNames.forEach((name,i)=>{if(mask&(1<<i))names.push('周'+name)});return names.join('、')}
async function loadSchedules(){try{const data=await api('/api/schedules');const list=document.getElementById('scheduleList');if(!data.schedules.length){list.className='empty';list.textContent='暂无任务';return}list.className='';list.innerHTML=data.schedules.map(s=>`<div class="schedule"><div class="row between"><div><div class="schedule-title">${String(s.hour).padStart(2,'0')}:${String(s.minute).padStart(2,'0')} · ${s.state.power?'开机':'关机'}</div><div class="schedule-meta">${daysText(s.days)} · ${s.state.mode} ${s.state.temp}℃ · 睡眠${s.state.sleepMode?` ${s.state.sleepMode}`:'关闭'} · ${s.enabled?'已启用':'已停用'}</div></div><div><button class="secondary" onclick="toggleSchedule(${s.id},${!s.enabled})">${s.enabled?'停用':'启用'}</button> <button class="secondary danger" onclick="deleteSchedule(${s.id})">删除</button></div></div></div>`).join('')}catch(e){toast(e.message)}}
async function addSchedule(){const [hour,minute]=document.getElementById('scheduleTime').value.split(':').map(Number);let mask=0;document.querySelectorAll('#days input:checked').forEach(el=>mask|=1<<Number(el.value));if(!mask){toast('至少选择一天');return}const scheduled={...state,power:document.getElementById('schedulePower').value==='on',temp:Number(tempSelect.value)};try{await api('/api/schedules',{method:'POST',body:JSON.stringify({enabled:true,days:mask,hour,minute,state:scheduled})});toast('任务已添加');loadSchedules()}catch(e){toast(e.message)}}
async function toggleSchedule(id,enabled){try{const all=await api('/api/schedules');const s=all.schedules.find(x=>x.id===id);s.enabled=enabled;await api('/api/schedules',{method:'POST',body:JSON.stringify(s)});loadSchedules()}catch(e){toast(e.message)}}
async function deleteSchedule(id){try{await api('/api/schedules?id='+id,{method:'DELETE'});loadSchedules()}catch(e){toast(e.message)}}
document.getElementById('addSchedule').onclick=addSchedule;
let presenceLoaded=false,presenceRequestIp='',presenceCloseDeadline=0,presenceClosePending=false,presenceAwayClosed=false,presenceAwayPowerOn=false;
function addPresenceDevice(device={}){const list=document.getElementById('presenceDevices');if(list.children.length>=4){toast('最多添加 4 部手机');return}const row=document.createElement('div');row.className='presence-device';const name=document.createElement('input');name.type='text';name.className='presence-name';name.maxLength=15;name.placeholder='名称';name.value=device.name||`手机 ${list.children.length+1}`;const ip=document.createElement('input');ip.type='text';ip.inputMode='decimal';ip.className='presence-ip';ip.placeholder='192.168.1.23';ip.value=device.targetIp||'';const remove=document.createElement('button');remove.type='button';remove.className='secondary danger';remove.textContent='移除';remove.onclick=()=>row.remove();row.append(name,ip,remove);list.append(row)}
function presenceDevicesFromForm(){return Array.from(document.querySelectorAll('.presence-device')).map(row=>({name:row.querySelector('.presence-name').value.trim(),targetIp:row.querySelector('.presence-ip').value.trim()}))}
function formatPresenceDuration(seconds){seconds=Math.max(0,Math.ceil(seconds));const minutes=Math.floor(seconds/60),rest=seconds%60;return minutes?`${minutes} 分 ${rest} 秒`:`${rest} 秒`}
function renderPresenceCountdown(){const el=document.getElementById('presenceCountdown');if(presenceClosePending){const seconds=Math.max(0,Math.ceil((presenceCloseDeadline-Date.now())/1000));el.hidden=false;if(seconds>0){const closeAt=new Date(presenceCloseDeadline);const time=closeAt.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'});el.textContent=`预计 ${time} 关闭空调 · 还剩 ${formatPresenceDuration(seconds)}`}else el.textContent='离家倒计时结束，正在关闭空调…'}else if(presenceAwayClosed){el.hidden=false;el.textContent='已判定离家 · 空调已关闭'}else if(presenceAwayPowerOn){el.hidden=false;el.textContent='已判定离家 · 空调仍在运行，等待执行关机'}else{el.hidden=true;el.textContent=''}}
async function loadPresence(){try{const d=await api('/api/presence');presenceRequestIp=d.requestIp||'';if(!presenceLoaded){document.getElementById('presenceEnabled').checked=d.enabled;document.getElementById('presenceAway').value=String(d.awaySeconds);document.getElementById('presenceAutoOn').checked=d.autoOn;document.getElementById('presenceAutoOff').checked=d.autoOff;document.getElementById('presenceDevices').replaceChildren();(d.devices||[]).forEach(addPresenceDevice);presenceLoaded=true}const names={home:'在家',away:'离家',unknown:'正在学习'};let detail=d.enabled?(names[d.status]||d.status):'未启用';const online=(d.devices||[]).filter(x=>x.online).map(x=>x.name);if(online.length)detail+=` · 在线：${online.join('、')}`;else if((d.devices||[]).length)detail+=' · 暂未检测到手机';document.getElementById('presenceStatus').textContent=detail;presenceClosePending=!!d.autoOffPending;presenceCloseDeadline=presenceClosePending?Date.now()+Number(d.awayRemainingSeconds||0)*1000:0;presenceAwayClosed=!!(d.enabled&&d.status==='away'&&!d.airConditionerPower);presenceAwayPowerOn=!!(d.enabled&&d.status==='away'&&d.autoOff&&d.airConditionerPower);renderPresenceCountdown()}catch(e){document.getElementById('presenceStatus').textContent='在家检测状态读取失败'}}
document.getElementById('addCurrentPhone').onclick=()=>{if(!presenceRequestIp){toast('暂时无法读取本机 IP');return}const devices=presenceDevicesFromForm();if(devices.some(x=>x.targetIp===presenceRequestIp)){toast('当前手机已经添加');return}addPresenceDevice({name:`手机 ${devices.length+1}`,targetIp:presenceRequestIp})};
document.getElementById('addBlankPhone').onclick=()=>addPresenceDevice();
async function savePresence(){const body={enabled:document.getElementById('presenceEnabled').checked,devices:presenceDevicesFromForm(),awaySeconds:Number(document.getElementById('presenceAway').value),autoOn:document.getElementById('presenceAutoOn').checked,autoOff:document.getElementById('presenceAutoOff').checked};try{await api('/api/presence',{method:'POST',body:JSON.stringify(body)});toast('自动控制已保存');presenceLoaded=false;loadPresence()}catch(e){toast(e.message)}}
document.getElementById('savePresence').onclick=savePresence;
async function loadHomeKit(){try{const d=await api('/api/homekit');document.getElementById('homekitStatus').textContent=d.paired?'已配对，可使用家庭与 Siri':(d.codeConfigured?'配对码已设置，等待加入家庭':'未配对，请先设置自己的配对码')}catch(e){document.getElementById('homekitStatus').textContent='HomeKit 状态读取失败'}}
async function saveHomeKitCode(){const input=document.getElementById('homekitCode');const code=input.value.trim();if(!/^\d{8}$/.test(code)){toast('请输入8位数字');return}try{await api('/api/homekit',{method:'POST',body:JSON.stringify({code})});input.value='';toast('配对码已保存');loadHomeKit()}catch(e){toast(e.message)}}
document.getElementById('saveHomekitCode').onclick=saveHomeKitCode;
async function syncClock(){try{await api('/api/time',{method:'POST',body:JSON.stringify({epoch:Math.floor(Date.now()/1000)})})}catch(e){}}
syncClock().then(loadState);loadSchedules();loadPresence();loadHomeKit();setInterval(loadState,3000);setInterval(renderPresenceCountdown,1000);setInterval(loadPresence,5000);setInterval(loadHomeKit,10000);
</script></body></html>
)rawliteral";
